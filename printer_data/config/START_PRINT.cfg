[gcode_macro START_PRINT]
description: Start print routine

gcode:
    {% set BED = params.BED_TEMP|default(60)|float %}
    {% set HOTEND = params.EXTRUDER_TEMP|default(240)|float %}

    # --- prep state ---
    G90                         ; absolute positioning
    M83                         ; relative extrusion

    # --- preheat nozzle ---
    M104 S{HOTEND * 0.8}
	
    # --- bring bed to temp ---
    M190 S{BED}
    G4 P20000                   ; small soak (20s, helps consistency)

    # --- home + mechanics ---
    G28
    Z_TILT_ADJUST
    G28 Z
    BED_MESH_CALIBRATE

    # --- move to safe height ---
    G1 Z10 F3000

    # --- bring Nozzle to temp ---
    M109 S{HOTEND}

    G4 P5000                   ; brief thermal stabilize
	
    # =========================
    # NOZZLE CLEAN MOVE (PURGE AREA ONLY)
    # =========================

    G92 E0

    ; move to purge start corner (same region as purge line)
    G1 X2 Y8 F8000

    ; small pressure reset
    G1 E1.2 F200

    ; micro-wipe along purge column (same X band as purge line)
    G1 X2 Y20 Z0.28 F3000
    G1 X2 Y45 F3000
    G1 X3 Y45 F3000
    G1 X3 Y20 F3000
    G1 X3 Y8 F3000

    # --- purge line ---
    G92 E0
    G1 X1 Y8 Z0.28 F6000

    G1 E5 F300
    G1 X1 Y52 E15 F1200
    G1 X4 Y52 F6000
    G1 X4 Y8 E15 F1200

    G92 E0