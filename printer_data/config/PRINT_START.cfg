#####################################################################
#   A better print_start macro for v2/trident
#####################################################################

## *** THINGS TO UNCOMMENT: ***
## Nevermore (if you have one)
## Z_TILT_ADJUST (For Trident only)

[gcode_macro PRINT_START]
gcode:
  # This part fetches data from your slicer. Such as bed, extruder, and chamber temps and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default("45")|int %}
  {% set target_chamber_minimal = params.CHAMBER_MINIMAL|default("0")|int %}
  {% set target_chamber_wait = [target_chamber, target_chamber_minimal]|select(">", 0)|min|default(0) %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

  # Clear any lingering pause states
  CLEAR_PAUSE

  # Home the printer, set absolute positioning and update the Stealthburner LEDs.
  # STATUS_HOMING      # Set LEDs to homing-mode
  SET_GCODE_OFFSET Z=0 ; Reset Z offset
  G28                                                   # Full home (XYZ)
  G90                                                   # Absolute position

  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if params.BED|int > 90 %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    # STATUS_HEATING                                      # Set LEDs to heating-mode
    M106 S255                                           # Turn on the PT-fan

    ##  Uncomment if you have a Nevermore.
    #SET_PIN PIN=nevermore VALUE=1                      # Turn on the nevermore

    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
    SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber_wait}c"  # Display info on display
    # TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={target_chamber_wait}   # Waits for chamber temp

  # If the bed temp is not over 90c, then skip the heatsoak and just heat up to set temp with a 5 min soak
  {% else %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    # STATUS_HEATING                                      # Set LEDs to heating-mode
    G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
    # SET_DISPLAY_TEXT MSG="Soak for 5 min"               # Display info on display
    # G4 P300000                                          # Wait 5 min for the bedtemp to stabilize
  {% endif %}

  # Heat hotend to 145c. This helps with getting a correct Z-home.
  SET_DISPLAY_TEXT MSG="Hotend: 145c"                   # Display info on display
  M109 S145                                             # Heat hotend to 145c

  #  Uncomment for Trident (Z_TILT_ADJUST)
  SET_DISPLAY_TEXT MSG="Leveling"                      # Display info on display
  # STATUS_LEVELING                                      # Set LEDs to leveling-mode
  Z_TILT_ADJUST                                        # Level the printer via Z_TILT_ADJUST
  CARTOGRAPHER_TOUCH_HOME

  M109 S{target_extruder}                # heat to print temp

  # Bed mesh while still at 145 (scan is contactless — less ooze than at print temp)
  SET_DISPLAY_TEXT MSG="Bed mesh"
  BED_MESH_CALIBRATE

  # ---- Work out the purge / park location ----
  {% set fl = params.LAYER|default(0.2)|float %}
  {% set purge_z = fl + 0.05 %}
  {% set front_y = params.PRINT_MIN_Y|default(-1)|float %}
  {% if front_y - 15 >= 4 %}
    {% set purge_y = front_y - 15 %}
  {% else %}
    {% set purge_y = 4 %}
  {% endif %}
  {% set purge_x = x_wait - 50 %}

  # Park right over the purge start and heat to print temp there
  SET_DISPLAY_TEXT MSG="Hotend: {target_extruder}c"
  G90
  G0 X{purge_x} Y{purge_y} Z10 F9000     # park above where the purge line begins
  M107                                   # part fan off
  #M109 S{target_extruder}                # heat to print temp while parked here

  # ---- Prime line ----
  SET_DISPLAY_TEXT MSG="Printer goes brr"
  G0 Z{purge_z}                          # drop straight down to prime height
  G91                                    # relative
  G1 E3 F300                             # short stationary prime
  G1 X100 E20 F1000                      # prime line
  G90                                    # absolute

  # # Heat up the hotend up to target via data from slicer
  # SET_DISPLAY_TEXT MSG="Hotend: {target_extruder}c"     # Display info on display
  # # STATUS_HEATING                                        # Set LEDs to heating-mode
  # G1 X{x_wait} Y{y_wait} Z15 F9000                      # Go to center of the bed
  # M107                                                  # Turn off partcooling fan
  # M109 S{target_extruder}                               # Heat the hotend to set temp

  # #  Uncomment for bed mesh
  # SET_DISPLAY_TEXT MSG="Bed mesh"                      # Display info on display
  # # STATUS_MESHING                                       # Set LEDs to bed mesh-mode
  # BED_MESH_CALIBRATE                                   # Start the bed mesh
  
  # # Purge Line
  # SET_DISPLAY_TEXT MSG="Printer goes brr"               # Display info on display
  # # STATUS_PRINTING                                       # Set LEDs to printing-mode
  # G0 X{x_wait - 50} Y{purge_y} F10000    # move to prime start, in front of the print
  # G0 Z{purge_z}                          # drop to prime height (first layer + 0.05)
  # G91                                    # relative positioning
  # G1 E5 F300                             # push a little extra out before the move
  # G1 X100 E20 F1000                      # prime line
  # G90                                    # absolute positioning